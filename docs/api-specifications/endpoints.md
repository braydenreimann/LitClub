## ASP.NET Core Web API
1. Perform server-side validation for **all inputs**

Containers & partition keys:
1. **Books** (partition key: `/bookId`)
2. **Threads** (partition key: `/bookId`)
3. **Posts** (partition key: `/threadId`)
4. **Users** (partition key: `/userId`)
